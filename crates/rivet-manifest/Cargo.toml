[package]
name = "rivet-manifest"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "rivet.toml design manifest"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
