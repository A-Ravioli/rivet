[package]
name = "rivet-hdl-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core executor, triggers, values, and backend trait for the Rivet hardware verification harness"
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
categories.workspace = true
keywords.workspace = true
readme = "README.md"

# The package is `rivet-hdl-core` on crates.io; the library keeps its
# short name, so testbenches say `rivet_core` as they always have.
[lib]
name = "rivet_core"

[dependencies]
inventory = { workspace = true }
log = { workspace = true }
regex-lite = { workspace = true }
